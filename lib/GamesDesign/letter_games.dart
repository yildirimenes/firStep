import 'package:flutter/material.dart';

class LetterGames extends StatefulWidget {
  const LetterGames({Key? key}) : super(key: key);

  @override
  State<LetterGames> createState() => _LetterGamesState();
}

class _LetterGamesState extends State<LetterGames> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white70,
      body: Column(

      ),
    );  }
}
